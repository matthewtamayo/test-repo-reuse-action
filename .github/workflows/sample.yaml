name: CDO-PR-GENERATOR

on:
  workflow_dispatch:
jobs:
  print:
    runs-on: ubuntu-latest
    steps:
      - name: print
        uses: matthewtamayo/sample-action@d6fba00013a5af9f04192c19a81e171e669d72d2
        with:
          messsage: "hello"
        # secrets: inherit

      - name: show repo
        shell: bash
        run: |
          chmod +x ./gh-rest-script.sh
        env:
          TOKEN: ${{ secrets.TOKEN }}
